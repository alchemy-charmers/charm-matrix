# exclude the interfaces and layers folders we use for submodules
exclude:
  - interfaces
  - layers
# include required layers here
includes:
  - layer:basic
  - layer:version
  - layer:snap
  - interface:reverseproxy
  - interface:pgsql
options:
  basic:
    python_packages:
      - signedjson
  snap:
    matrix-synapse:
      channel: stable
      devmode: false
      jailmode: false
      dangerous: false
      classic: false
      revision: null
  version:
    file_name: "repo-info"
repo: https://github.com/pirate-charmers/charm-matrix
